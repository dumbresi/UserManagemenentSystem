!#/bin/bash

sudo apt update
sudo apt upgrade -y
# install tar and wget to extract and install go
# sudo dnf install -y wget tar

# GO_VERSION="1.23.1"

# wget https://golang.org/dl/go${GO_VERSION}.linux-amd64.tar.gz

# # Clean up the downloaded tar file
# rm go${GO_VERSION}.linux-amd64.tar.gz

